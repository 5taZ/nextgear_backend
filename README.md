# Next Gear - Backend API

Backend сервер для Telegram Mini App магазина с базой данных PostgreSQL.

## 🚀 Технологии

- Node.js + Express
- TypeScript
- PostgreSQL (через Supabase)
- CORS для кросс-доменных запросов

## 📋 API Endpoints

### Users
- `POST /api/users` - Получить или создать пользователя

### Products
- `GET /api/products` - Получить все товары
- `POST /api/products` - Добавить товар (admin)
- `PATCH /api/products/:id` - Обновить товар (admin)
- `DELETE /api/products/:id` - Удалить товар (admin)

### Orders
- `GET /api/orders` - Получить все заказы (admin)
- `GET /api/orders/user/:userId` - Получить заказы пользователя
- `POST /api/orders` - Создать заказ
- `PATCH /api/orders/:id` - Обновить статус заказа (admin)

### Health Check
- `GET /health` - Проверка работоспособности сервера

## 🛠️ Локальная разработка

1. Установить зависимости:
```bash
npm install
```

2. Настроить переменные окружения:
```bash
cp .env.example .env
```

3. Отредактировать `.env`:
```
DATABASE_URL=postgresql://user:password@host:5432/database
PORT=3001
NODE_ENV=development
```

4. Запустить сервер:
```bash
npm run dev
```

Сервер запустится на http://localhost:3001

## 🚢 Развертывание

### Railway.app (Рекомендуется)

1. Создать новый проект на Railway
2. Подключить GitHub репозиторий
3. Добавить переменные окружения:
   - `DATABASE_URL`
   - `NODE_ENV=production`
4. Deploy произойдет автоматически

### Другие платформы

- Render.com
- Heroku
- DigitalOcean App Platform

## 📊 База данных

SQL схема находится в файле `schema.sql`. 

Чтобы создать таблицы в Supabase:
1. Откройте SQL Editor в Supabase Dashboard
2. Вставьте содержимое `schema.sql`
3. Нажмите Run

## 🔒 Безопасность

- CORS настроен для всех доменов (в продакшене ограничьте)
- Используется connection pooling для БД
- Транзакции для атомарных операций
- Graceful shutdown при остановке сервера

## 📝 Лицензия

MIT
